import UIKit

class ImageHelper {
    func resizeSquareImageForAvatarUsage(image: UIImage) {
        // expected avatar image : 120 x 120 in JPEG with less than 64 KiB
        let size = image.size
        if size.height != size.width {
            //
        }
    }
}
